{
    "scripts": {
        "start": "node server",
        "dev:start": "node server & cd client && npm start",
        "heroku-postbuild": "cd client && npm i && npm run build"
    },
    "dependencies": {
        "express": "^4.16.2",
        "node-cmd": "^3.0.0",
        "nodemon": "^1.12.1",
        "request": "^2.83.0"
    }
}
