{
    "scripts": {
        "start": "nvm use && node server",
        "dev:start": "node server & cd client && npm start",
        "heroku-postbuild": "cd client && npm i && npm run build"
    },
    "engines": {
        "node": "12.16.x"
    },
    "dependencies": {
        "aws-sdk": "^2.656.0",
        "axios": "^0.18.1",
        "dotenv": "^6.2.0",
        "express": "^4.17.1",
        "express-sslify": "^1.2.0",
        "node-cmd": "^3.0.0",
        "pg": "^7.18.2",
        "request": "^2.88.2",
        "request-promise": "^4.2.5",
        "skyscanner-promise-browse": "^1.0.3"
    }
}